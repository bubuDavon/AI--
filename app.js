'use strict';
const $=s=>document.querySelector(s), esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let state={profile:null,baseline:null,lessons:[],draft:{},attempts:[],final:null,events:[],plan:[],review:'',cycle:1}, view='overview', lessonIndex=0, demo=false, toastTimer, storageOK=true;
try{const saved=JSON.parse(localStorage.getItem('zdx-learning-v2'));if(saved&&saved.schema===2)state={...state,...saved};}catch{storageOK=false;}
const nav=[['overview','学习总览'],['baseline','课前摸底'],['learn','分层学习'],['practice','实操与反馈'],['final','结课测评'],['report','我的学情报告']];
function toast(s){$('#toast').textContent=s;clearTimeout(toastTimer);toastTimer=setTimeout(()=>$('#toast').textContent='',4200);}
function save(){if(demo)return;try{localStorage.setItem('zdx-learning-v2',JSON.stringify({...state,schema:2}));}catch{storageOK=false;$('#storageStatus').textContent='记录暂未保存，请导出';}}
function eventLog(label){state.events.push({label,time:new Date().toISOString()});save();}
function go(next){view=next;render();window.scrollTo({top:0,behavior:'instant'});}
function progress(){return [!!state.profile,!!state.baseline,state.lessons.length===3,state.attempts.at(-1)?.score===5,!!state.final,!!state.final&&state.plan.length>0];}
function nextView(){const a=progress(),i=a.findIndex(x=>!x);return ['overview','baseline','learn','practice','final','report'][i<0?5:i];}
function heading(title,desc,extra=''){return `<div class="heading"><div><div class="eyebrow">ZHIDUOXING / LEARNING JOURNEY</div><h1>${title}</h1><p>${desc}</p></div>${extra}</div>`;}
function routeStrip(){return `<ol class="route-strip">${['入学准备','能力摸底','学习实操','作品反馈','结课测评','复盘再练'].map((s,i)=>`<li class="${progress()[i]?'done':progress().findIndex(v=>!v)===i?'now':''}"><span>${progress()[i]?'✓':i+1}</span>${s}</li>`).join('')}</ol>`;}
function render(){document.title=(nav.find(n=>n[0]===view)?.[1]||({assistant:'知识助教',review:'教学复盘'}[view]))+' · 智多星';$('#learningNav').innerHTML=nav.map(([id,label],i)=>`<button data-go="${id}" class="${view===id?'active':''}" ${view===id?'aria-current="page"':''}><span class="num">${progress()[i]?'✓':i+1}</span>${label}</button>`).join('');$('#avatar').textContent=(state.profile?.alias||'同学').slice(0,2);$('#storageStatus').textContent=demo?'示例数据，不计入我的记录':storageOK?'● 本机自动保存':'记录暂未保存，请导出';$('#demoBanner').innerHTML=demo?'<div class="banner">正在体验虚构示例「小知」的学习过程。你的个人记录保持不变。 <button class="button ghost small" data-action="exitDemo">返回我的学习</button></div>':'';$('#content').innerHTML=({overview:overview,baseline:baselineView,learn:learnView,practice:practiceView,final:finalView,report:reportView,assistant:assistantView,review:reviewView}[view]||overview)();bindView();}
function overview(){const complete=progress().filter(Boolean).length;return heading(state.profile?`${esc(state.profile.alias)}，继续把 AI 用明白`:'从一次核验，开始学会用 AI','完成一堂体验课，带走你的核验作品与学习报告。',`<button class="button secondary" data-action="demo">看看完整示例 ↗</button>`)+routeStrip()+`<div class="columns"><div><section class="panel"><div class="row between"><span class="chip">${state.profile?'本轮学习':'第一步 · 入学准备'}</span><span class="muted tiny">约 20–30 分钟 · 可随时离开再继续</span></div><h2 style="margin-top:19px">${state.profile?'你的学习路线已保存':'先告诉智多星，你想学会什么'}</h2>${state.profile?`<p>学习方向：${esc(state.profile.audience)} · ${esc(state.profile.goal)}</p><div class="metrics"><div class="metric"><strong>${complete}<small>/6</small></strong><span>已完成环节</span></div><div class="metric"><strong>${state.attempts.length}</strong><span>实操提交次数</span></div><div class="metric"><strong>${state.final?state.final.score+' / 5':'—'}</strong><span>结课核验练习</span></div></div><div class="progress"><i style="width:${complete/6*100}%"></i></div><div class="step-actions"><span class="small muted">第 ${state.cycle} 轮 · 每次练习都保留记录</span><button class="button" data-go="${nextView()}">${state.final?'查看报告与下一步':'继续学习'} →</button></div>`:`<form id="profileForm"><div class="fieldgrid"><label class="field">学习昵称<input name="alias" placeholder="例如：小知（无需真实姓名）" maxlength="16" required></label><label class="field">我更接近<select name="audience"><option>大学生</option><option>零基础学习者</option><option>职场新人</option></select></label></div><label class="field">这次最想解决的问题<select name="goal"><option>判断 AI 回答和引用是否可靠</option><option>把学习或办公任务说清楚</option><option>安全地使用 AI，保护个人信息</option></select></label><label class="check"><input type="checkbox" name="consent" required><span>我会自己完成练习，不提交隐私或机密信息；使用 AI 后会核实来源，并按任务要求披露使用方式。</span></label><button class="button" type="submit">保存目标，开始摸底 →</button></form>`}</section><section class="panel"><div class="row between"><h2>这堂课，你会交付什么</h2><span class="chip purple">学习闭环</span></div><ul class="lesson-list"><li><span class="index">01</span><div class="body"><strong>一份可追溯的核验记录</strong><p>把「我觉得对」变成主张、来源、证据和结论。</p></div></li><li><span class="index">02</span><div class="body"><strong>一轮收到反馈后的修改</strong><p>检查作品结构，针对缺口补充，再提交。</p></div></li><li><span class="index">03</span><div class="body"><strong>一份学习报告与下一轮计划</strong><p>保留前后测分数、提交记录和具体改进行动。</p></div></li></ul></section></div><div><section class="panel soft"><div class="eyebrow">TODAY'S TOPIC</div><h2 style="margin-top:10px">AI 说得很像真的，<br>怎样判断才可靠？</h2><p class="small muted">从一条模拟 AI 引用开始，练习定位原始来源、识别证据缺口，再把过程写清楚。</p><div class="pills"><span>事实核验</span><span>清晰提问</span><span>安全边界</span></div><div class="divider"></div><p class="tiny muted">这是一堂可操作的学习体验。测评自动计分，开放作品提供规则检查；事实判断仍需你核实或讲师复核。</p></section><section class="panel"><h3>你的随身资料库</h3><p class="small muted">72 张知识卡 · 6 张知识图<br>分层课程与公开学习材料</p><a class="button secondary small" href="knowledge.html">打开完整知识库 ↗</a></section><section class="panel"><h3>记录保存在这里</h3><p class="small muted">当前浏览器自动保存，尚未跨设备同步。完成后可下载报告；清理浏览器数据会影响本机记录。</p><button class="button ghost small" data-action="export">备份我的学习记录 ↓</button></section></div></div>`;}
function locked(message,target,label){return heading('先完成前一步','每个环节都使用你上一阶段留下的学习证据。')+`<section class="panel empty"><h2>${message}</h2><button class="button" data-go="${target}">${label} →</button></section>`;}
function grade(items,answers){return {answers,score:items.reduce((n,q,i)=>n+(q.a===answers[i]?1:0),0),verification:items.slice(0,5).reduce((n,q,i)=>n+(q.a===answers[i]?1:0),0),at:new Date().toISOString()};}
function questionHTML(q,i,answers,submitted=false){const chosen=answers?.[i];return `<fieldset class="question"><legend><span class="tag">${String(i+1).padStart(2,'0')} / ${esc(q.tag||'当堂验收')}</span>${esc(q.q)}</legend>${q.o.map((o,j)=>`<label class="option ${submitted?(j===q.a?'correct':j===chosen?'wrong':''):''}"><input type="radio" name="q${i}" value="${j}" ${chosen===j?'checked':''} ${submitted?'disabled':'required'}><span>${esc(o)}</span></label>`).join('')}${submitted?`<div class="explain">${chosen===q.a?'✓ 判断正确':'需要再看一眼'} · ${esc(q.why)}</div>`:''}</fieldset>`;}
function weakCards(){return state.baseline?BASELINE.filter((q,i)=>state.baseline.answers[i]!==q.a).map(q=>q.card):[];}
function baselineView(){if(!state.profile)return locked('先保存你的学习目标','overview','填写入学准备');const result=state.baseline;return heading('课前摸底','10 道练习：5 道核验判断 + 5 道使用方法。用于推荐学习重点，不判断学习动机。')+(result?`<section class="panel soft"><div class="columns"><div><span class="chip">摸底已完成 · D2</span><h2 style="margin-top:15px">${result.verification<3?'先练稳核验步骤，再挑战复杂任务':'基础判断较稳，重点练习证据记录'}</h2><p class="small muted">${weakCards().length?'优先复习：'+weakCards().map(id=>esc(KNOWLEDGE.nodes.find(n=>n.id===id)?.name||id)).join('、'):'全部判断正确。接下来用实操作品检查是否真正会做。'}</p><button class="button" data-go="learn">进入推荐学习 →</button></div><div class="metrics"><div class="metric"><strong>${result.verification}/5</strong><span>核验判断</span></div><div class="metric"><strong>${result.score-result.verification}/5</strong><span>使用方法</span></div></div></div></section>`:'<p class="note">这是公开体验题，不是正式课程的独立验收题。答案提交后显示；摸底结果保留到本轮结束。</p>')+`<section class="panel"><form id="quizForm" data-kind="baseline">${BASELINE.map((q,i)=>questionHTML(q,i,result?.answers||state.baselineDraft,result!=null)).join('')}${result?'<button type="button" class="button" data-go="learn">带着结果去学习 →</button>':'<button class="button" type="submit">提交摸底，生成学习建议 →</button>'}</form></section>`;}
function cardHTML(id){const n=KNOWLEDGE.nodes.find(n=>n.id===id);if(!n)return '';return `<article class="knowledge-card"><span class="id">KNOWLEDGE CARD / ${n.id}</span><h3>${esc(n.name)}</h3><p class="small">${esc(n.definition)}</p><ul>${n.points.map(p=>`<li>${esc(p)}</li>`).join('')}</ul><div class="example"><strong>练一遍：</strong>${esc(n.example)}<br><strong>检查学会没有：</strong>${esc(n.assessment)}</div><p class="tiny muted">参考来源：${(n.sources||[]).map(id=>{const source=KNOWLEDGE.sources.find(s=>s.id===id);return source?.url?`<a href="${esc(source.url)}" target="_blank" rel="noopener noreferrer">${esc(source.title)}</a>`:esc(id);}).join(' · ')}</p></article>`;}
function learnView(){if(!state.baseline)return locked('先完成课前摸底','baseline','进入摸底');const lesson=LESSONS[lessonIndex],done=state.lessons.includes(lessonIndex),low=state.baseline.verification<3;return heading('分层学习','沿着“识别 → 核验 → 表达”练一遍。每小节通过一个检查点后，留下完成记录。')+`<div class="tabs">${LESSONS.map((l,i)=>`<button data-lesson="${i}" class="${i===lessonIndex?'selected':''}">${state.lessons.includes(i)?'✓':i+1}　${['识别边界','留下证据','清楚表达'][i]}</button>`).join('')}</div><div class="columns"><section class="panel"><span class="chip">第 ${lessonIndex+1} 小节 · ${lesson.time}</span><h2 style="margin-top:16px">${lesson.title}</h2>${lesson.cards.map(cardHTML).join('')}<div class="divider"></div><h3>现在试一试</h3><p>${esc(lesson.task)}</p><form id="lessonForm">${questionHTML({...lesson,tag:'当堂检查'},0,[],false)}<button class="button" type="submit">${done?'再检查一次':'检查答案，记录完成'} →</button></form></section><div><section class="panel soft"><h3>${esc(state.profile.audience)} · ${low?'保底路线':'标准路线'}</h3><p class="small">${state.profile.audience==='零基础学习者'?'一次只做一步。先读卡片里的例子，做完后再进入检查点。':state.profile.audience==='职场新人'?'将练习迁移到会议纪要或业务汇报，尤其检查数字、责任人和信息来源。':'将练习迁移到论文引用、课程汇报或简历材料。核心论证与判断由你完成。'}</p><p class="small muted">${low?'先把原始来源与检索边界练熟。分层只反映这次知识表现，不评价自驱力。':'可以增加挑战：为一条冲突主张写出两份来源的差异和适用范围。'}</p></section><section class="panel"><h3>学习进度</h3><ul class="lesson-list">${LESSONS.map((l,i)=>`<li><span class="index">${state.lessons.includes(i)?'✓':i+1}</span><div class="body"><strong>${['识别边界','留下证据','清楚表达'][i]}</strong><p>${state.lessons.includes(i)?'检查点已完成':'待完成检查点'}</p></div></li>`).join('')}</ul><button class="button ${state.lessons.length===3?'':'secondary'}" data-go="practice" style="margin-top:15px">${state.lessons.length===3?'进入实操任务':'查看接下来的实操'} →</button></section><section class="panel"><h3>卡住了？</h3><p class="small muted">查方法、找例子、确认边界。</p><button class="button ghost small" data-go="assistant">向知识助教提问 →</button></section></div></div>`;}
function checks(d){const quote=(d.evidence||'').replace(/[\s“”"。]/g,''),original=MATERIAL_A.replace(/[\s“”"。]/g,'');return [
{name:'定位直接来源',ok:d.source==='A',detail:d.source==='A'?'选择了直接讨论这条主张的材料 A。':'请区分原文、检索记录和 AI 自证。材料 A 才是本题的直接依据。'},
{name:'摘录可对照的证据',ok:quote.length>=10&&original.includes(quote),detail:quote.length>=10&&original.includes(quote)?'摘录能在本题材料 A 中找到。':'请摘录材料 A 中支持判断的一段连续原文，至少 10 个字；不要改写成不存在的证据。'},
{name:'结论与本题材料一致',ok:d.verdict==='conflict',detail:d.verdict==='conflict'?'原文要求核对出处，与题目中的“不需要核对”冲突。':'对照“不需要核对”与材料 A 的“需核对”。本题应判断为与原文冲突。'},
{name:'说明判断理由',ok:(d.reason||'').trim().length>=25,detail:(d.reason||'').trim().length>=25?'已写出理由；仅检查了长度，论证质量仍待人工复核。':'请至少用 25 个字说明主张与证据的关系。字数只是完整性提示，不代表论证质量。'},
{name:'披露使用方式与限制',ok:(d.disclosure||'').trim().length>=10,detail:(d.disclosure||'').trim().length>=10?'已填写使用方式与限制；尚未核实其真实性。':'请说明是否使用 AI、自己完成了哪一步，以及这次判断的适用范围（至少 10 字）。'}];}
function practiceView(){if(state.lessons.length<3)return locked('完成三个学习检查点后，再进入实操','learn','继续学习');const d=state.draft||{},last=state.attempts.at(-1);return heading('实操与反馈','对照材料核验一条主张，提交记录；按反馈修改，再提交一次。',`<span class="chip">D3 · 第 ${state.attempts.length+1} 次提交</span>`)+`<div class="columns"><div><section class="panel"><span class="chip purple">模拟材料 · 仅用于本页练习</span><h2 style="margin-top:15px">这条 AI 主张，证据支持吗？</h2><div class="example">${PRACTICE_CLAIM}</div><details open><summary>材料 A · 模拟手册节选 / 第 2 段</summary><p class="small">${MATERIAL_A}</p><button class="button ghost small" data-action="quote">将这段原文放入证据栏 ↓</button></details><details><summary>材料 B · 模拟检索记录</summary><p class="small">在单一数据库用书名检索，未命中。记录没有覆盖其他数据库或替代关键词。</p></details><details><summary>材料 C · 模拟 AI 再次确认</summary><p class="small">AI 再次回答：“我确认刚才的说法是对的。”没有附原始出处。</p></details><p class="tiny muted">以上书名、主张和检索记录均为原创模拟素材，不表示真实出版物或真实检索结果。</p></section><section class="panel"><h2>提交你的核验记录</h2><form id="practiceForm"><label class="field">1. 你使用哪份直接证据？<select name="source" required><option value="">请选择材料</option>${[['A','材料 A · 手册节选'],['B','材料 B · 检索记录'],['C','材料 C · AI 再次确认']].map(([v,t])=>`<option value="${v}" ${d.source===v?'selected':''}>${t}</option>`).join('')}</select></label><label class="field">2. 摘录原文证据<textarea name="evidence" maxlength="1200" placeholder="摘录能直接支持判断的原文，保留出处。" required>${esc(d.evidence)}</textarea></label><label class="field">3. 当前判断<select name="verdict" required><option value="">请选择判断</option>${[['conflict','这条主张与材料 A 的原文冲突'],['unknown','当前材料不足，暂不能判断'],['supported','这条主张已被原文支持']].map(([v,t])=>`<option value="${v}" ${d.verdict===v?'selected':''}>${t}</option>`).join('')}</select></label><label class="field">4. 为什么这样判断？<textarea name="reason" maxlength="2000" placeholder="对照主张和证据，解释具体哪里一致或冲突（至少 25 字）。" required>${esc(d.reason)}</textarea></label><label class="field">5. 使用披露与适用范围<textarea name="disclosure" maxlength="1000" placeholder="说明是否使用 AI、自己做了什么；本次判断只依据哪些材料（至少 10 字）。" required>${esc(d.disclosure)}</textarea></label><button class="button" type="submit">提交记录，查看反馈 →</button><p class="tiny muted">输入会保存到当前浏览器，不上传班级后台。不要填写隐私或机密信息。</p></form></section></div><div><section class="panel ${last?'':'soft'}" id="feedback"><div class="row between"><h2>智多星的反馈</h2><span class="chip">规则检查</span></div>${last?`<div class="score">${last.score}<small> / 5 项</small></div><p class="small muted">第 ${state.attempts.length} 次提交 · ${last.score===5?'本题检查项齐全':'请补充下方缺口'}</p><ul class="rubric">${last.checks.map(c=>`<li><span class="status ${c.ok?'':'pending'}">${c.ok?'✓':'○'}</span><div><strong class="small">${c.name}</strong><p>${c.detail}</p></div></li>`).join('')}</ul><p class="note">前三项对照本题固定材料；后两项仅做填写完整性提示。系统未核实真实世界事实，也未进行 AI 语义批改。</p><button class="button ${last.score<5?'secondary':''}" data-go="final">${last.score<5?'补齐 5 项后可进入测评':'进入结课测评'} →</button>`:'<p class="small muted">提交后，这里会逐项检查来源、摘录、判断、理由和使用披露，并给出具体修改动作。</p><div class="note">反馈不是最终结论。你需要对照材料修改；开放作品的质量仍需人工复核。</div>'}</section>${state.attempts.length?`<section class="panel"><h3>你的修改轨迹</h3><ul class="timeline">${state.attempts.map((a,i)=>`<li><strong class="small">第 ${i+1} 次 · ${a.score}/5 项</strong><p>${new Date(a.at).toLocaleString('zh-CN')} · ${a.checks.filter(c=>!c.ok).map(c=>c.name).join('、')||'检查项齐全，理由质量待复核'}</p></li>`).join('')}</ul></section>`:''}</div></div>`;}
function finalView(){if(!state.attempts.length||state.attempts.at(-1).score<5)return locked('先补齐实操记录的五个检查项','practice','修改实操作品');const r=state.final;return heading('结课测评','5 道平行核验练习，与你的课前核验得分使用相同的 0–5 分量尺。')+(r?`<section class="panel soft"><div class="row between"><div><span class="chip">${r.score>=3?'达到本页练习目标':'建议补练后再试'}</span><div class="score" style="margin-top:12px">${r.score}<small> / 5</small></div><p class="small muted">课前核验 ${state.baseline.verification}/5 → 本次 ${r.score}/5。这里只记录练习表现，不能单独证明实际工作能力。</p></div><button class="button" data-go="report">生成我的学情报告 →</button></div></section>`:'<p class="note">本页体验目标为 ≥3/5，是教学建议而非正式认证。试题与答案公开，不能用作保密考试。</p>')+`<section class="panel"><form id="quizForm" data-kind="final">${FINALQUIZ.map((q,i)=>questionHTML(q,i,r?.answers||state.finalDraft,r!=null)).join('')}${r?'<div class="row"><button type="button" class="button" data-go="report">查看报告 →</button><button type="button" class="button secondary" data-action="retryFinal">再练一次</button></div>':'<button class="button" type="submit">提交测评，生成报告 →</button>'}</form></section>`;}
function suggestions(){const wrong=state.final?FINALQUIZ.filter((q,i)=>state.final.answers[i]!==q.a):[];return [...new Set([...(wrong.length?wrong.map(q=>`复习「${KNOWLEDGE.nodes.find(n=>n.id===q.card)?.name}」，另找一条主张重做核验。`):['独立核验一条真实 AI 引用，记录原始出处与主张对应关系。']),'请讲师复核我的判断理由与使用披露，并记录修改建议。','把核验清单迁移到下一次学习或办公任务，保留过程证据。'])];}
function reportView(){if(!state.final)return locked('完成结课测评后，生成个人报告','final','进入测评');const r=state.final,b=state.baseline,delta=r.score-b.verification;return heading('我的学情报告','看见本轮表现，也明确下一轮具体要做什么。',`<button class="button secondary" data-action="reportDownload">下载报告 ↓</button>`)+`<section class="panel"><div class="row between report-title"><div><span class="eyebrow">LEARNING REPORT / D5</span><h2 style="margin:8px 0 3px">${esc(state.profile.alias)} · 第 ${state.cycle} 轮学习</h2><span class="small muted">${esc(state.profile.audience)} · ${new Date(r.at).toLocaleDateString('zh-CN')} ${demo?'· 虚构示例':''}</span></div><div class="badge-circle">${r.score>=3?'✓':'↗'}</div></div><div class="columns"><div><h3>${r.score>=3?'你已完成这次核验学习体验':'这次流程已走完，接下来重点补练'}</h3><p class="muted small">${r.score>=3?'本次达到公开体验题的建议目标。接下来，用真实任务和人工复核检查迁移效果。':'先回看错题对应知识卡，再用一份新的材料重做核验。分数不会替代你的学习目标。'}</p><div class="metrics"><div class="metric"><strong>${b.verification}/5</strong><span>课前核验</span></div><div class="metric"><strong>${r.score}/5</strong><span>本次结课</span></div><div class="metric"><strong>${delta>0?'+':''}${delta}</strong><span>核验得分变化</span></div></div><div class="pills"><span>3 个学习检查点</span><span>${state.attempts.length} 次作品提交</span><span>${(state.finalAttempts||[]).length+1} 次结课练习</span></div></div><div><h3>同量尺前后对照</h3><div class="bars" aria-label="课前 ${b.verification} 分，本次 ${r.score} 分，满分五分"><div class="bar" style="height:${Math.max(4,b.verification/5*85)}px"><span>${b.verification}/5</span></div><div class="bar new" style="height:${Math.max(4,r.score/5*85)}px"><span>${r.score}/5</span></div></div><div class="barlabels"><span>课前核验</span><span>本次结课</span></div></div></div><p class="note">前后题目为本页编写的平行练习，尚未经测量等值验证；重复练习存在熟悉效应。没有班级样本、人工验收和真实工时，不能据此推算教学成效或 AI 承接率。</p></section><div class="columns"><section class="panel"><h2>把下一步写进行动</h2><p class="small muted">选中至少一项，完成本轮“学习 → 反馈 → 再练”的闭环。</p>${suggestions().map((s,i)=>`<label class="check"><input type="checkbox" data-plan="${i}" value="${esc(s)}" ${state.plan.includes(s)?'checked':''}><span>${esc(s)}</span></label>`).join('')}<div class="step-actions"><span class="chip">${state.plan.length?'已保存 '+state.plan.length+' 项计划':'等待选择下一步'}</span><button class="button ${state.plan.length?'':'secondary'}" data-go="review">查看教学复盘 →</button></div></section><section class="panel"><h2>我的学习证据</h2><ul class="timeline">${state.events.slice(-7).map(e=>`<li><strong class="small">${esc(e.label)}</strong><p>${new Date(e.time).toLocaleString('zh-CN')}</p></li>`).join('')}</ul><div class="row"><button class="button secondary small" data-action="export">备份记录 ↓</button><button class="button ghost small" data-action="print">打印报告</button></div></section></div>`;}
let chatResult='',chatQuestion='';
function assistantView(){return heading('知识助教','基于已有知识卡检索方法与来源；未连接外部大模型，不生成未经核验的答案。')+`<div class="columns"><section class="panel"><div class="row"><span class="brandmark" style="width:32px;height:32px;font-size:18px">知</span><strong>智多星在这里</strong><span class="chip">知识库检索</span></div><div class="chat-bubble">你卡在哪一步？可以问“搜索不到文献怎么办”“怎样避免上传隐私”或“提示词怎么写”。我会给你相关知识卡和可复做的方法。</div><form id="assistantForm"><label class="field">你的问题<input name="query" value="${esc(chatQuestion)}" placeholder="输入关键词或问题…" maxlength="200" required></label><button class="button" type="submit">查找方法 →</button></form><div class="row" style="margin-top:15px">${['搜索不到文献','如何保护隐私','怎么写提示词'].map(s=>`<button class="button secondary small" data-ask="${s}">${s}</button>`).join('')}</div>${chatResult}</section><div><section class="panel soft"><h3>先把卡点说具体</h3><ol class="small" style="padding-left:20px"><li>你想完成什么？</li><li>刚才做到了哪一步？</li><li>屏幕或材料显示什么？</li><li>你已经试过什么？</li></ol><p class="small muted">不需要账号密码、身份证、学号或完整个人材料。</p></section><section class="panel"><h3>需要人工接手时</h3><p class="small muted">涉及真实作业判分、重要事实冲突、账号安全或个人情绪时，记录问题并请讲师复核。</p><button class="button secondary small" data-action="handoff">生成待复核记录</button><p class="tiny muted">只生成本机记录，不会自动发送给老师。</p></section></div></div>`;}
function answer(query){chatQuestion=query;let ids=[];if(/代写|帮我写.*(论文|作业)|直接提交/.test(query)){chatResult='<div class="chat-bubble"><strong>我可以帮你拆解方法和检查问题。</strong><p>需要独立完成的核心作业应由你自己完成；先说明任务要求，我可以给出核验清单。</p></div>'+cardHTML('B10');}
else{if(/文献|引用|搜.*不到|未命中|核验/.test(query))ids=['B08','B05','B07'];else if(/隐私|个人信息|身份证|学号|安全/.test(query))ids=['B09','A12','A10'];else if(/提示词|提问|任务|怎么问/.test(query))ids=['C01','C02','C03'];else if(/幻觉|真假|准确|自信/.test(query))ids=['B03','B06','B12'];else ids=KNOWLEDGE.nodes.map(n=>({id:n.id,score:query.split(/\s+/).reduce((s,t)=>s+(t.length>1&&(n.name+' '+n.definition+' '+n.points.join(' ')).includes(t)?1:0),0)})).filter(x=>x.score>0).sort((a,b)=>b.score-a.score).slice(0,3).map(x=>x.id);chatResult=ids.length?'<div class="chat-bubble">找到了这些相关方法。请结合你的材料核实；这些是预先编写的教学卡，不是实时联网回答。</div>'+ids.map(cardHTML).join(''):'<div class="chat-bubble">暂未命中相关知识卡。请缩短为一个关键词，或记录问题请讲师复核。我不会据此编造答案。</div>';}
go('assistant');}
function reviewView(){const status=[['D1','使用边界',!!state.profile],['D2','课前基线',!!state.baseline],['D3','学习与实操',state.lessons.length===3&&state.attempts.length>0],['D4','结课练习',!!state.final],['D5','个人报告',!!state.final],['D6','复盘与案例',!!state.final&&state.plan.length>0]];return heading('教学复盘','复盘当前浏览器内的一名学习者、一轮体验。D1–D6 是交付物编号，不是上课天数。',`<button class="button secondary" data-action="caseDownload">导出本轮案例 ↓</button>`)+`<div class="columns"><div><section class="panel"><div class="row between"><h2>交付物检查</h2><span class="chip">${demo?'虚构示例':'本机体验记录'}</span></div><ul class="lesson-list">${status.map(([id,t,ok])=>`<li><span class="index">${id}</span><div class="body"><strong>${t}</strong><p>${ok?'已有本轮记录':'待完成相应学习环节'}</p></div><span class="chip ${ok?'':'amber'}">${ok?'已记录':'待完成'}</span></li>`).join('')}</ul></section><section class="panel"><h2>哪些地方还需要人工复核</h2><ul class="small"><li>开放作品中，理由是否充分、披露是否真实。</li><li>能否把本页模拟材料中的方法用于真实任务。</li><li>实际课堂工时、全班出勤与提交分母仍未采集。</li></ul>${(state.handoffs||[]).map(h=>`<p class="note">待复核：${esc(h.question)}<br><span class="tiny">${new Date(h.at).toLocaleString('zh-CN')} · 未发送</span></p>`).join('')}<form id="reviewForm"><label class="field">本轮发现的问题与下一次改进<textarea name="review" maxlength="3000" placeholder="例如：把“搜索未命中”的练习提前，增加一次原文与转述对照。">${esc(state.review)}</textarea></label><button class="button secondary small" type="submit">保存复盘</button></form></section></div><div><section class="panel soft"><h2>完整闭环进度</h2><div class="score">${progress().filter(Boolean).length}<small> / 6</small></div><p class="small muted">${progress().every(Boolean)?'已完成本轮流程，并保存下一步计划。':'完成结课报告并选择下一步行动，才算本轮闭环。'}</p><div class="progress"><i style="width:${progress().filter(Boolean).length/6*100}%"></i></div><div class="step-actions"><button class="button" data-go="${nextView()}">${progress().every(Boolean)?'回看我的报告':'继续未完成环节'} →</button></div></section><section class="panel"><h3>开始下一轮</h3><p class="small muted">保留本轮报告和记录，重新摸底、学习与练习。题目会重复，因此新分数仅作练习记录。</p><button class="button secondary small" data-action="newCycle" ${!progress().every(Boolean)?'disabled':''}>归档本轮，开始再练 ↻</button></section>${(state.archive||[]).length?`<section class="panel"><h3>历史轮次</h3>${state.archive.map(a=>`<p class="small">第 ${a.cycle} 轮 · 核验 ${a.baseline?.verification??'—'} → ${a.final?.score??'—'}/5<br><span class="muted tiny">已随备份记录保留</span></p>`).join('')}</section>`:''}<p class="tiny muted">正式班级运行仍需接入身份与权限、班级数据存储、真实工具和讲师审核。本页不会伪造 AI 承接率、全班通过率或运营成效。</p></div></div>`;}
function bindView(){const f=$('#profileForm');if(f)f.onsubmit=e=>{e.preventDefault();const d=new FormData(f),alias=String(d.get('alias')).trim();if(!alias){toast('请输入一个学习昵称');return;}state.profile={alias,audience:d.get('audience'),goal:d.get('goal'),consent:true};eventLog('保存学习目标与使用边界');go('baseline');};
const quiz=$('#quizForm');if(quiz&&!state[quiz.dataset.kind]){quiz.onchange=()=>{const d=new FormData(quiz),items=quiz.dataset.kind==='baseline'?BASELINE:FINALQUIZ;state[quiz.dataset.kind+'Draft']=items.map((_,i)=>d.has('q'+i)?Number(d.get('q'+i)):null);save();};quiz.onsubmit=e=>{e.preventDefault();const d=new FormData(quiz),kind=quiz.dataset.kind,items=kind==='baseline'?BASELINE:FINALQUIZ;state[kind]=grade(items,items.map((_,i)=>Number(d.get('q'+i))));if(kind==='final')state.plan=[];eventLog(kind==='baseline'?'完成课前摸底：核验 '+state.baseline.verification+'/5':'完成结课练习：'+state.final.score+'/5');render();window.scrollTo({top:0,behavior:'smooth'});};}
const lesson=$('#lessonForm');if(lesson)lesson.onsubmit=e=>{e.preventDefault();const answer=Number(new FormData(lesson).get('q0'));if(answer!==LESSONS[lessonIndex].a){toast(LESSONS[lessonIndex].why);return;}if(!state.lessons.includes(lessonIndex)){state.lessons.push(lessonIndex);eventLog('通过学习检查点：'+LESSONS[lessonIndex].title);}toast('检查点通过，已记录。');if(lessonIndex<2)lessonIndex++;else go('practice');render();window.scrollTo({top:0,behavior:'smooth'});};
const practice=$('#practiceForm');if(practice){practice.oninput=()=>{state.draft=Object.fromEntries(new FormData(practice));save();};practice.onsubmit=e=>{e.preventDefault();state.draft=Object.fromEntries(new FormData(practice));const result=checks(state.draft);state.attempts.push({at:new Date().toISOString(),data:{...state.draft},checks:result,score:result.filter(c=>c.ok).length});eventLog('提交实操作品，第 '+state.attempts.length+' 次');render();$('#feedback').scrollIntoView({behavior:'smooth',block:'start'});toast('反馈已生成，请逐项查看。');};}
const ask=$('#assistantForm');if(ask)ask.onsubmit=e=>{e.preventDefault();answer(String(new FormData(ask).get('query')).trim());};const review=$('#reviewForm');if(review)review.onsubmit=e=>{e.preventDefault();state.review=String(new FormData(review).get('review')).trim();eventLog('保存教学复盘');toast('复盘已保存');};}
let personalState=null;
function enterDemo(){if(demo){go('report');return;}personalState=JSON.parse(JSON.stringify(state));demo=true;const draft={source:'A',evidence:MATERIAL_A,verdict:'conflict',reason:'题目说不需要核对原始出处，但材料 A 明确要求核对题名、作者、年份与原始出处，两者在核对要求上相互冲突。',disclosure:'本记录为虚构示例，只依据页面模拟材料核验，没有查询真实出版物。'};const c=checks(draft),now=new Date().toISOString();state={profile:{alias:'小知（示例）',audience:'大学生',goal:'判断 AI 回答和引用是否可靠',consent:true},baseline:grade(BASELINE,[1,0,0,1,0,1,0,1,1,2]),lessons:[0,1,2],draft,attempts:[{at:now,data:{...draft,evidence:'我认为不对'},checks:checks({...draft,evidence:'我认为不对'}),score:4},{at:now,data:draft,checks:c,score:5}],final:grade(FINALQUIZ,[1,2,0,1,0]),events:[{label:'示例：完成摸底、三个检查点与两轮修改',time:now},{label:'示例：完成结课练习 4/5',time:now}],plan:['请讲师复核我的判断理由与使用披露，并记录修改建议。'],review:'示例复盘：下一轮重点补练对不确定性的表达。',cycle:1,archive:[]};go('report');}
document.addEventListener('click',e=>{const el=e.target.closest('[data-go],[data-action],[data-lesson],[data-ask]');if(!el)return;if(el.dataset.go){go(el.dataset.go);return;}if(el.dataset.lesson!=null){lessonIndex=Number(el.dataset.lesson);render();return;}if(el.dataset.ask){answer(el.dataset.ask);return;}const action=el.dataset.action;
if(action==='export')downloadJSON();else if(action==='demo')enterDemo();else if(action==='exitDemo'){state=personalState;demo=false;personalState=null;go('overview');}else if(action==='quote'){const ta=$('textarea[name="evidence"]');ta.value=MATERIAL_A;ta.dispatchEvent(new Event('input',{bubbles:true}));ta.scrollIntoView({behavior:'smooth',block:'center'});toast('原文已放入证据栏，请自己完成判断与说明。');}else if(action==='retryFinal'){state.finalAttempts=[...(state.finalAttempts||[]),state.final];state.final=null;state.finalDraft=[];state.plan=[];eventLog('开始重复结课练习（已有结果保留）');render();}else if(action==='reportDownload')downloadText('智多星学情报告.md',reportText());else if(action==='caseDownload')downloadText('智多星本轮体验案例.md',caseText());else if(action==='print')window.print();else if(action==='handoff'){if(!chatQuestion){toast('请先输入需要复核的问题');return;}state.handoffs=[...(state.handoffs||[]),{question:chatQuestion,at:new Date().toISOString()}];eventLog('记录一个待人工复核问题（未发送）');toast('已保存到教学复盘，未自动发送。');}else if(action==='newCycle'){if(!progress().every(Boolean))return;const {archive=[],...snapshot}=state;state={profile:snapshot.profile,baseline:null,lessons:[],draft:{},attempts:[],final:null,events:[],plan:[],review:'',cycle:snapshot.cycle+1,archive:[...archive,snapshot]};eventLog('归档上一轮，开启第 '+state.cycle+' 轮');go('baseline');}});
document.addEventListener('change',e=>{if(e.target.matches('[data-plan]')){const item=e.target.value;state.plan=e.target.checked?[...new Set([...state.plan,item])]:state.plan.filter(p=>p!==item);eventLog('更新下一轮练习计划');render();}});
function downloadText(name,text,type='text/markdown;charset=utf-8'){
 const previous=document.getElementById('exportDialog');if(previous)previous.remove();
 const blob=new Blob([text],{type}),url=URL.createObjectURL(blob),dialog=document.createElement('dialog');dialog.id='exportDialog';dialog.setAttribute('aria-label','导出学习资料');
 dialog.innerHTML=`<h2>内容已生成</h2><p class="small muted">保存文件，或复制下面的完整内容。部分内置浏览器可能不支持下载。</p><label class="field">${esc(name)}<textarea readonly rows="10" style="min-height:240px">${esc(text)}</textarea></label><div class="row"><a class="button" id="saveExport" download="${esc(name)}">保存文件 ↓</a><button class="button secondary" id="copyExport">复制全部内容</button><button class="button ghost" id="closeExport">关闭</button></div>`;
 document.body.appendChild(dialog);dialog.querySelector('#saveExport').href=url;dialog.querySelector('#copyExport').onclick=async()=>{try{await navigator.clipboard.writeText(text);toast('内容已复制');}catch{dialog.querySelector('textarea').select();toast('请使用复制快捷键复制所选内容');}};dialog.querySelector('#closeExport').onclick=()=>dialog.close();dialog.addEventListener('close',()=>{URL.revokeObjectURL(url);dialog.remove();});dialog.showModal();
}
function downloadJSON(){downloadText('智多星学习记录'+(demo?'_示例':'')+'.json',JSON.stringify({...state,schema:2,dataMode:demo?'虚构示例':'本机学习记录'},null,2),'application/json');}
function reportText(){if(!state.final)return '# 尚未完成结课测评\n请先完成本轮学习。';return `# 智多星学情报告${demo?'（虚构示例）':''}\n\n学习昵称：${state.profile.alias}\n方向：${state.profile.audience} / ${state.profile.goal}\n轮次：${state.cycle}\n记录类型：${demo?'虚构示例，不能作为真实学员成绩':'当前浏览器学习记录，未经人工身份与表现核验'}\n\n## 前后表现\n课前核验：${state.baseline.verification}/5\n课前使用方法：${state.baseline.score-state.baseline.verification}/5\n本次结课核验：${state.final.score}/5\n实际提交次数：${state.attempts.length}\n重复结课练习次数：${(state.finalAttempts||[]).length}\n\n## 作品与反馈\n${state.attempts.map((a,i)=>`第 ${i+1} 次：${a.score}/5 项。${a.checks.map(c=>c.name+'：'+(c.ok?'已满足本页检查条件':'待补充')).join('；')}\n证据：${a.data.evidence}\n理由：${a.data.reason}\n披露：${a.data.disclosure}`).join('\n\n')}\n\n## 下一步\n${state.plan.length?state.plan.map(p=>'- '+p).join('\n'):'尚未选择下一步。'}\n\n## 适用范围\n这是公开体验练习，不是正式能力认证。平行题尚未经等值验证，重复作答有熟悉效应。开放作品的理由质量和披露真实性需人工复核。未采集班级数据与实际工时，不计算 AI 承接率或班级成效。\n`;}
function caseText(){return reportText()+`\n## 本轮教学复盘（D6 体验版）\n${state.review||'尚未填写复盘。'}\n\n## 事件记录\n${state.events.map(e=>'- '+e.time+' '+e.label).join('\n')}\n\n## 待人工复核\n${(state.handoffs||[]).map(h=>'- '+h.question+'（未发送）').join('\n')||'开放作品的理由与披露仍需讲师核查。'}\n\n只有当前浏览器内的本轮记录，不代表真实班级样本。\n`;}
$('#sharePage').onclick=async()=>{if(location.protocol==='file:'||['localhost','127.0.0.1'].includes(location.hostname)){toast('这是本机预览，公开网址发布后可分享。');return;}try{await navigator.clipboard.writeText(location.origin+location.pathname);toast('网页链接已复制；不会分享你的本机学习记录');}catch{toast('请复制浏览器地址栏的网址分享');}};
render();
